using System;


namespace AlchemyMiniGame
{
    internal class Artefact: AlchemyEquipment
    {
        public Artefact(string name, Effect[] effects, int id) : base(name, effects, id)
        {
        }
    }
}
